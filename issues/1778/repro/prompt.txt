Reply with exactly the text between the BEGIN and END markers, verbatim, character for character (keep every line break), with nothing else before or after it. Do not add code fences.

BEGIN
Before the formula.

$$T_{\text{appearance}\rightarrow\text{chunk}}
\approx73\text{--}146\text{ ms}$$

## Content after the formula

- This should remain a list item.
- [This should remain a link](https://example.com).
END
