Verifier run for #2190 (worktree wf_21e66a79-f02-11, base fcada5a3b, own instance ports 16384/24384/32384, data dir deleted).
- Part A reproduced: bb.db=4096 after start, after project create (287 frames WAL-only), and after SIGTERM (bb.db still 4096, WAL grew).
- Walkthrough, pinned-reader, exit-durability, vitest (3 passed) all reproduced with matching output.
- Refutation: walkthrough step 6 "bb.db mtime changed: true" is produced by an explicit
  db.$client.pragma("wal_checkpoint(PASSIVE)") in the harness. Re-running step 6 without that line:
  "bb.db mtime changed: false". bb never runs that pragma on its own (only SQLite's 1000-page
  auto-checkpoint, or the hourly sweep when freelist >= 1024 pages).
- All cited code locations verified at fcada5a3b; origin/main (15f21ade7) has no fix.
