diff --git a/apps/app/src/components/plugin/PluginPendingInteractionComposer.tsx b/apps/app/src/components/plugin/PluginPendingInteractionComposer.tsx
index eecb562d6..38c12d8e0 100644
--- a/apps/app/src/components/plugin/PluginPendingInteractionComposer.tsx
+++ b/apps/app/src/components/plugin/PluginPendingInteractionComposer.tsx
@@ -1,4 +1,4 @@
-import { useCallback, useMemo, useState } from "react";
+import { useCallback, useMemo, useRef, useState } from "react";
 import { Button } from "@bb/shared-ui/button";
 import type { JsonValue, PendingInteraction } from "@bb/domain";
 import { PluginSlotMount } from "./PluginSlotMount";
@@ -6,6 +6,7 @@ import { resolvePendingInteraction } from "@/lib/plugin-slot-resolvers";
 import { usePluginSlots } from "@/lib/plugin-slots";
 import { useStopThread } from "@/hooks/mutations/thread-runtime-mutations";
 import { sdk } from "@/lib/sdk";
+import { useStickyFooterAvailableHeight } from "@/components/thread/user-questions/useStickyFooterAvailableHeight";
 
 /** The plugin form to mount: the plugin, its renderer, and the ask. */
 export interface PluginPendingInteractionRequest {
@@ -38,6 +39,8 @@ export function PluginPendingInteractionComposer({
   const stopThread = useStopThread();
   const [error, setError] = useState<string | null>(null);
   const [submitting, setSubmitting] = useState(false);
+  const rootRef = useRef<HTMLElement>(null);
+  const availableHeight = useStickyFooterAvailableHeight(rootRef);
   const slot = useMemo(
     () =>
       resolvePendingInteraction(
@@ -89,8 +92,16 @@ export function PluginPendingInteractionComposer({
   const dismissLabel = dismissal === "cancel" ? "Cancel" : "Stop turn";
 
   return (
-    <section className="mb-2 rounded-lg border border-border bg-surface-recessed px-4 py-3 text-xs text-muted-foreground">
-      <header className="mb-4 min-w-0">
+    <section
+      ref={rootRef}
+      className="mb-2 flex min-h-0 min-w-0 flex-col rounded-lg border border-border bg-surface-recessed px-4 py-3 text-xs text-muted-foreground"
+      style={
+        availableHeight === null
+          ? undefined
+          : { maxHeight: `${availableHeight}px` }
+      }
+    >
+      <header className="mb-4 min-w-0 shrink-0">
         <h3 className="text-pretty text-sm font-semibold text-foreground">
           {request.title}
         </h3>
@@ -120,7 +131,7 @@ export function PluginPendingInteractionComposer({
             </div>
           }
         >
-          <fieldset disabled={submitting}>
+          <fieldset className="contents" disabled={submitting}>
             <slot.component
               interaction={{
                 id: interaction.id,
diff --git a/apps/app/src/components/thread/pending-interactions/ThreadPendingInteractionBanner.tsx b/apps/app/src/components/thread/pending-interactions/ThreadPendingInteractionBanner.tsx
index 94e0766d8..07c4c7ebe 100644
--- a/apps/app/src/components/thread/pending-interactions/ThreadPendingInteractionBanner.tsx
+++ b/apps/app/src/components/thread/pending-interactions/ThreadPendingInteractionBanner.tsx
@@ -137,6 +137,7 @@ export function ThreadPendingInteractionBanner({
       // cancelled; a provider's plugin-defined request ends with its turn.
       return (
         <div
+          className="min-w-0"
           data-testid="plugin-request-banner"
           data-request-kind={request.kind}
         >
